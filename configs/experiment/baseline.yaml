# Baseline Experiment
experiment:
  name: baseline_v1
  description: "Initial XGBoost baseline with standard features"
  tags:
    - baseline
    - xgboost
    - fpl
    
# Override defaults for baseline
model:
  learning_rate: 0.1
  max_depth: 6
  n_estimators: 100
  
data:
  rolling_window: 5
  
training:
  early_stopping_rounds: 30
